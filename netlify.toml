[build]
  command = "node build.js"
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Mirror the Vercel /api/* convention so the app code is unchanged
[[redirects]]
  from = "/api/groq"
  to   = "/.netlify/functions/groq"
  status = 200
